name: snitxpense
description: Voice-based daily expense tracker
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.6.0
  intl: ^0.19.0
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
